# Miyabi Optimization Prompt Generator

このプロンプトは、miyabiを最大効率化させるためのプロンプトを生成します。

## 目的

miyabi（Autonomous Development Framework）を使用する際に、最も効率的な結果を得るためのプロンプトを自動生成します。

## 使用方法

以下のテンプレートを使用してmiyabiに指示を出します：

```
Issue #{issue_number}を実装してください。

要件:
- タスクタイプ: {task_type} (fix-bug | add-feature | refactor | deploy | review-pr)
- 実行モード: {mode} (auto | guided | safe)
- 承認レベル: {approval} (auto | critical | all)

期待される成果:
{expected_outcomes}

制約事項:
{constraints}
```

## 最適化のポイント

1. **明確なタスク定義**: Issue番号とタスクタイプを明示的に指定
2. **適切なモード選択**:
   - `auto`: 完全自動（実験的機能に適用）
   - `guided`: デフォルト推奨（重要な判断時に確認）
   - `safe`: 安全優先（本番環境への変更）

3. **承認レベルの調整**:
   - `auto`: すべて自動承認
   - `critical`: 重要な操作のみ確認（推奨）
   - `all`: すべての操作を確認

## 例

### バグ修正
```bash
npx miyabi run -i 42 -t fix-bug -m guided -a critical
```

### 新機能追加
```bash
npx miyabi run -i 123 -t add-feature -m auto -a auto --verbose
```

### リファクタリング
```bash
npx miyabi run -i 456 -t refactor -m safe -a all
```

## ベストプラクティス

- Issue本文に詳細な要件を記載する
- 期待される成果を明確にする
- テストケースを事前に定義する
- レビュー基準（品質スコア80点以上）を意識する

---

🌸 Generated for Miyabi Optimization
